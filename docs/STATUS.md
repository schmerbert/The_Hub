# The Hub - Current Status

This page is the canonical map of the current runtime. New readers should begin with [`ORIENTATION.md`](ORIENTATION.md) for the proposition, reasons, overall shape, and intended direction, then use [`GLOSSARY.md`](GLOSSARY.md) for clinical mappings. Specifications under `docs/specs/` are cumulative slices: an older slice's non-goals describe that slice, not necessarily the present runtime. When specifications conflict, the precedence table below controls. [`HOUSE_GRAMMAR_V1.md`](specs/HOUSE_GRAMMAR_V1.md) is the active constitutional vocabulary and status-labeled design index; it installs no structure or capability and does not supersede narrower implementation specifications.

Current behavior is determined by the verified runtime plus the current owning specification, not by one preserved declaration string read in isolation. Exact installed topology and event text may remain historical ancestry because changing it would falsify hashes or replay. The language actually shown to the Resident is a separately witnessed presentation layer mapped in [`RESIDENT_PRESENTATION.md`](RESIDENT_PRESENTATION.md). Any intentional mismatch between preserved ancestry and current presentation is compatibility debt and must be registered, tested, and removed only through a versioned migration.

## Implemented now

- A plain shared `BUILDER_CHANNEL.md` correspondence surface. The Resident can read it from the Workshop repository and append an exact, stable-ID problem report through ordinary workbench custody; the Builder can reply with assessment or disposition. It is not automatic context, World authority, Forest memory, or a notification service.
- The standalone Workshop Continuity Seed is imported as an inert room-owned package surface. It provides a generation-1 SQLite operational store and bounded Cockpit projection for attributed objectives, facts, decisions, questions, Git observations, drift, and Kiln references. It is not yet mounted as a Resident tool or automatic wake source and stores no conversation, hidden reasoning, embeddings, or semantic Forest material.
- A forward-only Roots boundary for non-respiratory causal evidence. House Hearth packets remain rooted as before. Exact provider reasoning is stored once by content hash with causal edges to its Spine return, wake, and Session Scroll row. New assistant Scroll rows retain visible speech/action plus a typed pointer; active same-wake tool chains carry only a provider-required empty reasoning field rather than reinjecting the rooted body. Historical reasoning and Roots pointers do not enter the API Scroll, and reasoning is never Forest-eligible.
- A forward-only Session Scroll trace epoch. Inherited history remains exact without invented ancestry; after `scroll_trace_boundary/v1`, every new Scroll row is atomically paired with an append-only source/gate/witness/destination/disposition manifest. Glass-ground, Forest-Exhale, Patch-Bay, and provisional-batch trace manifests remain visible future work.
- A bounded memory-only provisional intake collector before stream Scrub. It coalesces only within request/phase/channel, preserves channel order, flushes normal completions, discards paused failure/cancellation material, and prevents raw fragments from reaching persistence or display. Corner batches safe stream rendering to one animation frame.
- A forward-only Glass trace epoch. Every new cast item resolves to stable code-owned Glass, an exact ground receipt, a Session Scroll coordinate, or a Source event; Scrub disposition and provider ordinal close the path into the exact Spine request. Crossing, verified World, attention, continuity, and Patch Bay/schema mount receipts are append-only and atomically bound to the cast. Wake inspection exposes the complete packet walk.
- One process-lived resident lifespan per server start, with Session Zero ancestry and a native two-breath `tend_hearth` handshake on the first human turn. Before the forced action, both situated ground and the capability schema explain that tending retrieves bounded, attributable continuity kept for the lifespan; retrieval does not endorse the return or prove memory.
- Exact Source Ledger, provider-presentation and provider-return Scrub receipts, hash-linked Spine custody, and optional verified Forest Home/Wild intake. Home verification proves one exact atom per eligible user/resident utterance; Wild verification proves one exact span per eligible receipt-backed Workshop read/search result. An append-only Forest Intake Ledger records body-free source offers and `admitted`/`held`/terminal decision history, preserves attempted Home predecessor position, and exposes held/unresolved health counts. `forest:plan` / `forest:apply`, `forest:wild:plan` / `forest:wild:apply`, and `forest:intake:plan` / `forest:intake:apply` are separate confirmation-gated catch-up paths; retries are idempotent.
- Glass Casting v1 is the implemented provider presentation. On the first wake it supplies minimal physics and exact current ground, then carries the exact `tend_hearth` call and House Hearth return causally. After successful tending, later provider rounds pin only the occupied ten-slot Silver Bullet holster directly beneath stable Glass and above the deferred fold; Ember orientation, prior-session excerpts, horizons, and blank slots are not reinjected.
- A persistent, event-projected World Graph with the Hub, Center, Workshop, Garden, House, Threshold, and literal House Hearth; per-lifespan location; five engageable Workshop fixtures; a stateful front door; a turnable Garden stone; visible non-traversable Forest/Road boundaries; a complete Ceiling catalog; and location-based Patch Bay mounting. New lifespans begin in the House before the Hearth; earlier lifespans retain their event-time origin. Fixture focus fits attention without changing World authority.
- Workshop fixture focus is one replaceable working focus. The whole room remains authoritative, while provider-facing schemas are fitted to the focused fixture on the next continuation; engaging another fixture switches directly without a disengage step. The stored v1 Workshop topology wording predates this fitting behavior and remains hash ancestry; current presence and action descriptions state the live law.
- The first `tend_hearth` return is deterministic bounded Markdown carrying the Ember lineage orientation, three adopted Silver Bullets in a ten-slot rack with seven invisible blanks, and exact bounded prior-session excerpts with Forest pointers when available.
- Bounded repository exploration and mutation, local Git operations, work briefs, timers, approvals, and deterministic Corner Step Slips. Search, regex search, and glob now traverse eligible files incrementally, skip declared dependency/build/cache directories, and return exact partial results with examined/skipped counts and an honest boundary instead of refusing an ordinary large scope before evaluation. Reads disclose total lines and the next exact range; mounted schemas advertise configured limits. The Workshop catalog includes a bounded mechanical repository overview and distinguishes installed tools from those immediately fitted.
- Result Rack custody for ordinary Gateway success, refusal, pending approval, and approval-completion crossings. Exact machine-result bytes/manifests are retained in SQLite and resident tool returns use deterministic `result_rack_projection_v1` host-return Scrub projections. Asynchronous recipe completion is also captured.
- Recoverable Result Exhale. An older completed tool exchange may leave immediate attention only when every result has a verified same-lifespan Result Rack projection. Glass carries the full bounded pointer-only trail-sign set once per causal human wake; later tool continuations carry one deterministic exact-pointer continuation marker instead of replaying every full sign. The universal host-owned `reopen_result` affordance deliberately reopens one exact stored projection without World action, room, approval, execution, or mutation authority. Every visible sign, continuation marker, and reopening is retained in append-only Roots exposure custody with exact Glass/Scrub/Spine ancestry and is explicitly non-respirable and non-Forest-eligible.
- Workshop action rounds are legible inside the wake. Each action-capable provider continuation receives used/remaining round ground, and the configured boundary reserves one schema-free final opportunity for Resident speech. A provider tool call emitted despite that absent mount is refused and the wake fails honestly; the host never fabricates completion.
- Provider attention fitting: a persisted per-phase attention meter, health projection, fitted resident tool schemas keyed by the engaged fixture, and declared whole-message omission of older completed tool-call/result exchanges. Current and configured recent exchanges remain; every omission is recorded by provider-presentation Scrub with a disclosure while the exact original history remains in host custody. World tool authority remains unchanged.
- The workbench fits sandbox diff preview beside sandbox promotion, so the Resident is never offered the confirmation path without its exact candidate view.
- Append-only approval custody. A pending confirm-class call records a pending approval receipt linked to its original action receipt; confirmed, rejected, and restart-cancelled decisions create distinct completion action and approval receipts. Promotion confirmation is therefore separate from the initial pending result.
- Live-mode named recipes run in one persistent job-scoped Docker Sandbox Bay backed by a disposable Git worktree. The canonical checkout is not mounted read-write, container networking is `none`, the child environment is minimal, resource caps are declared, and backend failure refuses without host fallback.
- `workshop_sandbox_diff` and always-confirm `workshop_sandbox_promote`. Promotion is plan- and patch-hash bound, requires a clean and unchanged canonical checkout, refuses symlink/submodule and Hub control paths, applies only through the host-owned crossing, records completion custody, and resets the sandbox job after success.
- The complete Workshop implementation is source-coherent under `src/places/hub/workshop/`: declaration, repository adapter, Git, recipes, Sandbox Bay, promotion, and protected-path law. Historical World and path-law imports are thin compatibility doors. This is not yet dynamic room discovery or automatic installation; World, Ceiling, Gateway, Scrub, and custody crossings remain explicit host wiring.
- `room-installation.v1` validates inert room installation requests. Workshop carries the first standing `room.json`. A deterministic host-owned `room-installation-witness.v1` hashes the exact manifest and verifies Workshop placement, parent containment, entrance, fixtures, Ceiling, mounting, schemas, Gateway handlers, approval classes, sockets, and custody; bounded Builder World inspection exposes it and names every loose wire. The manifest cannot self-install, self-authorize, create its door, supply its sockets, or install custody. Generic discovery, dynamic admission, transactional wiring, disable/removal, package provenance, and marketplace machinery are not implemented.
- A forward-only `room-installation-receipt.v1` ledger binds exact verified room witnesses to exact World events, manifest hashes, host bindings, admission statements, and installation time. Workshop is retained as `inherited_pre_boundary` with its pre-boundary admission honestly unrecorded. The ledger witnesses installation history; it does not dynamically install rooms or provide lifecycle machinery.
- The Corner has a read-only expandable Marble Inspector backed by the exact installation witness. Opening World inspection widens the working surface while keeping conversation present; the Inspector navigates overview, topology, each Workshop affordance, sockets, custody, hashes, and loose-wire state, with raw World data retained as a separate tab. It is the first expandable task surface, not yet a general browser/chart/notepad panel framework.
- The Result Rack implementation is split across `src/result-rack/`, with `src/world/results.js` retained as a compatibility facade. Runtime code imports Result Rack and attention owners directly, and the World Gateway dispatches through a complete registry checked against the installed Ceiling catalog.
- `WakeService` owns wake orchestration. DeepSeek responses stream over provider SSE; bytes admitted under the 8 MiB default `HUB_PROVIDER_MAX_RETURN_BYTES` ceiling are retained exactly in the Spine and independently assembled by provider-return Scrub before terminal use. Oversized returns seal the exact admitted prefix as incomplete custody and fail closed.
- Durable append-only `wake_stream_events` custody and a bounded process event bus. Safe provisional thinking/content/tool-name deltas, receipt-backed tool/approval/result cards, and terminal events are persisted before broadcast. Tool argument fragments are withheld, and credential-shaped provisional channels are suppressed without changing final provider custody. `GET /api/events` exposes same-origin named SSE events; `GET /api/events/history` is the recovery authority when replay requires resynchronization. Corner keeps polling as a fallback.
- Live Corner thinking, phase-bound speech, tool-call, approval, diff, and result views derived from provisional provider envelopes or committed host receipts/Result Rack projections. Exact mid-call speech is sealed as a durable **Resident · en route** step and remains ordered across later tool phases and after completion; it does not become a canonical utterance or Forest atom. Only the terminal scrubbed message enters conversation history and ordinary Forest admission.
- An Electron 43.2.0 Corner shell launched by `npm run desktop`: single instance, owned loopback Hub lifecycle, secure preload/IPC boundary, 96x96 compact and 980x680 expanded geometry at a 24-pixel display margin, tray expand/collapse/quit, ordinary-close hiding, configurable normal always-on-top, and renderer parity with the browser.

## Specification precedence

Later adopted slices supersede only the named parts of earlier slices; remaining custody and constitutional laws stay in force.

| Current authority | Supersedes |
| --- | --- |
| `GLASS_CASTING_V1.md` | The active provider-presentation shape and the Longshore Current continuity-name / wake-blessing roles in Wake Ritual, Session/Hearth, and Hearth Scroll. Historical records and custody remain unchanged. |
| `SESSION_HEARTH_V1.md` and `HEARTH_SCROLL_V2.md` | First Breath / Orientation Receipt / Wake Ritual presentation timing and the original large Hearth return |
| `WORLD_GRAPH_WORKSHOP_V1.md` | Earlier claims that no rooms or movement exist |
| `THRESHOLD_HOUSE_GARDEN_V1.md` | The Center/Workshop-only topology and door-only movement surface; B1 adds the exact versioned exterior shell and passage laws while deferring Threshold wake chronology and writing law. |
| `WORKSHOP_TOOL_CEILING_V1.md` | Station-to-tool mounting and the read-only Workshop capability ceiling |
| `WORKSHOP_FIXTURES_V1.md` | Living Spec Table and Control Panel stations; fixture engagement replaces station engagement |
| `WORKSHOP_HEARTBEAT_V1.md` | Leave-room recipe cancellation; kiln and timer are house-bound within the lifespan |
| `CEILING_PATCH_BAY_V1.md` | Ad hoc tool-list projection; the Ceiling catalog and room profiles own mounting |
| `CORNER_STEP_SLIPS_V1.md` | A single undifferentiated active-wake gap |
| `WORKSHOP_SANDBOX_BAY_V1.md` | Direct-host recipe execution in live mode and promotion without an isolation boundary |
| `WORKSHOP_RESULT_RACK_V1.md` | Unfitted ordinary host returns and unreceipted removal of old completed tool exchanges |
| `WAKE_STREAM_V1.md` | Polling-only active-wake presentation and non-streaming provider-return assumptions |
| `CORNER_DESKTOP_V1.md` | The web-only wrapper boundary in `CORNER_SURFACE.md` section 2 |
| `HOUSE_HEARTH_WAKE_V1.md` | B1 Center-start law, passive first-wake Glass inheritance, and the prior Hearth presentation |

The Glass row describes the current runtime presentation. Session/Hearth v1 and Hearth Scroll v2 remain implementation ancestry and custody law where Glass does not supersede them. `WORKSHOP_CONTROL_PANEL_V1.md` and `WORKSHOP_STATIONS_V1.md` likewise remain implementation ancestry. Use them only where a later Workshop specification has not superseded their claims.

## Implemented core, broader design still partial

- [`THRESHOLD_HOUSE_GARDEN_V1.md`](specs/THRESHOLD_HOUSE_GARDEN_V1.md): B1 is implemented and verified. An exact append-only topology extension installs the Hub container, Garden, House, Threshold, Center/Garden opening, stateful Garden/House front door, House/Threshold passage, visible non-traversable Forest/Road boundaries, globally persistent door and stone state, a turnable Garden stone, and inert Center marker/House window. Journal-bearing A2 stores require the explicit read-only-by-default, backup-confirmed `world:migrate-b1` path; startup never upgrades or repairs them. Threshold wake chronology, marker/window writing law, inventory, and boundary crossing remain deferred B2 work.
- [`WORKSHOP_SANDBOX_BAY_V1.md`](specs/WORKSHOP_SANDBOX_BAY_V1.md): live Docker execution, disposable worktree custody, no host fallback, diff inspection, and hash-bound approval-gated promotion are implemented. Durable backend-neutral lifecycle events, immutable brief-revision binding, restart recovery of an external sandbox, independent repository metadata, and broad artifact/resource telemetry remain design work.
- [`WORKSHOP_RESULT_RACK_V1.md`](specs/WORKSHOP_RESULT_RACK_V1.md): append-only jobs/status/output/artifact/projection rows, exact pointers, deterministic bounded projections, ordinary Gateway integration, async recipe completion, provider attention measurement, and declared old-tool-exchange omissions are implemented. General sandbox file-artifact collection, Rack list/read tools, retention/disposal policy, parser-derived observations, and complete promotion-package artifact custody are not.

Result Rack currently retains exact machine-result JSON and output chunks plus their manifests. It must not be read as a claim that arbitrary files produced inside a sandbox are collected or durably retained.

## Workshop trust and safety boundary

Workshop paths remain relative to the configured root and refuse traversal, symlinks, `.git`, `.runtime`, environment files, and credential-like paths. The resident cannot supply an arbitrary shell command. Network Git and destructive/rewriting Git operations are not installed.

Live mode requires `HUB_SANDBOX_BACKEND=docker`. The configured `HUB_SANDBOX_IMAGE` must already be present locally: jobs run with network disabled and the Hub does not pull an image as an execution fallback. If Docker, the image, worktree provisioning, or a required control is unavailable, recipe invocation refuses; it never runs that live recipe in `HUB_WORKSHOP_ROOT`.

Fake mode is different by design: it retains the legacy direct-host RecipeRunner for tests and demonstration. That runner executes trusted workspace code with the Hub process's filesystem permissions and is not an OS sandbox. `HUB_SANDBOX_BACKEND=host-test` is accepted only with fake configuration, but `createHub` does not instantiate the HostTestSandboxBackend. Do not use fake recipe execution for untrusted repositories.

`HUB_APPROVAL_MODE=auto` is a fake-mode test convenience. Live mode keeps confirmation-class actions gated, and sandbox promotion remains confirmation-gated even in auto approval mode. Pending approvals belong to the lifespan that created them and are cancelled rather than confirmable after restart. A kiln left `running` by process termination is reconciled as cancelled.

For a confirm-class call, the initial action receipt may be `committed` because creation of the pending approval was the immediate committed result. The later decision and mutation have their own append-only action and approval receipts linked to the pending phase.

## Adopted design, not implemented
- [`SECURITY_PRIVACY_CUSTODY_V1.md`](specs/SECURITY_PRIVACY_CUSTODY_V1.md): adopted security architecture and readiness gates for classification, sealed-pointer custody, encryption/key lifecycle, bounded sensitive attention, direct Vault intake, remote devices, migrations, destruction, and independent review. No application-level encryption at rest, authentication, secret manager, sealed Vault blob store, remote device admission, cryptographic erasure, or security certification is implemented today.
- [`HOUSE_GRAMMAR_V1.md`](specs/HOUSE_GRAMMAR_V1.md): active constitutional vocabulary, including the Recursive Forest Law. Every Forest repeats Home, Wild, and Mycelium at its own scale; a whole Forest may participate as one member at a wider scale without rewriting its interior jurisdictions or laundering leaf authority. This adopts grammar only, not recursive substrates, traversal frames, portals, Mycelium storage, or a `place.forest` projection runtime.
- [`FOREST_PATHS_ROLLING_FOLD_V1.md`](specs/FOREST_PATHS_ROLLING_FOLD_V1.md): adopted traversal and context-waterfall design. Conversation chronology is a hardlined structural trail; embeddings offer possible directions; the Resident's choices lay paths while unchosen candidates remain latent at their junctions. An active warm tether permits intentional walk-back and exact return without changing World presence. Rolling-fold generation, embeddings, traversal, Faun entry, context rollover, and walk-back are not implemented.
- [`WORLD_EVENT_PROJECTION_DRIFT_V1.md`](specs/WORLD_EVENT_PROJECTION_DRIFT_V1.md): A1 and A2 are implemented and verified. The hash-linked World journal and deterministic projector now cover physical state plus kiln runtime, timers, retained brief revisions, approvals, and exact event-linked custody. Legacy physical/operational boundaries are explicit; A1-only journal upgrades are backup-gated and never run by startup. External approval effects enter durable non-retryable `applying` state before execution. House/Garden work may now build on this pipe.
- [`FOREST_EXHALE_V2.md`](specs/FOREST_EXHALE_V2.md): recoverable Result trail signs, deliberate bounded reopening, and Roots exposure custody are implemented. Semantic Forest selection remains uninstalled and shadow-only until its downwind behavior is reviewed and explicitly enabled.
- [`OUTSIDE_ORGANS_DIRECTION_V1.md`](specs/OUTSIDE_ORGANS_DIRECTION_V1.md): adopts The Binder as the intended portfolio Window authority, The Box as the Marble-shaped outside observation supplier and public watering hole, and The Pipes as deliberate named Road fittings. None is connected to the Hub. Passive retained observation, active/paid refresh, public service, portfolio mutation, and web passage remain separate future crossings.
- `VAULT_V1.md`: the Vault room, door, document graph, wings, bin/slot crossing, principle documents, and Forest-to-Vault pointers.

## Current streaming and desktop boundaries

- Durable wake events use monotonic, append-only, hash-linked custody and persist before process broadcast. The process replay buffer is an optimization. Its `resync_required` envelope is synthesized non-durable transport control, not a journal row; Corner preserves its last contiguous cursor and pages the durable history endpoint before reconnecting.
- Provider deltas are provisional (`authority=provider_provisional`, `committed=false`). Host lifecycle/tool/card events are receipt-derived. The terminal scrubbed assistant message is the only provider message admitted to canonical history and downstream authority.
- Disconnecting EventSource does not cancel, pause, or backpressure an active wake. User cancellation is not implemented, and the 400 ms persisted-slip poll remains the renderer fallback. Process shutdown does abort the active provider crossing, allows 250 ms for partial raw/outcome custody, then gates late callbacks before closing stores. Tool and World execution outside that provider window remains awaited.
- DeepSeek SSE bytes are parsed as they arrive, but the Spine appends one terminal raw-return frame rather than one record per network chunk. Normal and interrupted crossings retain the complete admitted bytes; an oversized crossing retains only the exact bounded prefix admitted before the offending chunk and records incomplete capture.
- The desktop shell has automated geometry, lifecycle, and security coverage. Native GUI/tray smoke verification remains pending, and no installer, signing, update, or packaging pipeline exists yet.
- Corner does not currently provide general file/artifact opening, editor navigation, or source highlighting. Result and diff cards are bounded textual projections with exact custody pointers.

## Deferred

The partial Sandbox Bay and Result Rack items named above; sealed-pointer custody, encryption/key lifecycle, authentication, security migrations, cryptographic erasure, and independent review; implementation of the adopted Forest Paths/Rolling Fold design; semantic Forest Exhale beyond recoverable Result trail signs; recursive Forest substrates, frames, Mycelium, and portals; Vault runtime; remaining Threshold/House/Garden wake and writing laws; Backpack/threads; the passive Binder Window projection; Box retained/active observation crossings; Pipes atlas/live-knock crossings; other Forest/Road crossings; reset UI; free-form shell; companion/delegation bridges; multi-root workspaces; autonomous model wakes; user cancellation; streaming backpressure; per-chunk Spine custody; desktop packaging/installers; general file/artifact opening and highlighting; external publishing, deployment, and network Git operations.
