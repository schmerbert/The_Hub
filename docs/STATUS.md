# The Hub - Current Status

This page is the canonical map of the current runtime. New readers should begin with [`ORIENTATION.md`](ORIENTATION.md) for the proposition, reasons, overall shape, and intended direction, then use [`GLOSSARY.md`](GLOSSARY.md) for clinical mappings. Specifications under `docs/specs/` are cumulative slices: an older slice's non-goals describe that slice, not necessarily the present runtime. When specifications conflict, the precedence table below controls. [`HOUSE_GRAMMAR_V1.md`](specs/HOUSE_GRAMMAR_V1.md) is the active constitutional vocabulary and status-labeled design index; it installs no structure or capability and does not supersede narrower implementation specifications.

Current behavior is determined by the verified runtime plus the current owning specification, not by one preserved declaration string read in isolation. Exact installed topology and event text may remain historical ancestry because changing it would falsify hashes or replay. The language actually shown to the Resident is a separately witnessed presentation layer mapped in [`RESIDENT_PRESENTATION.md`](RESIDENT_PRESENTATION.md). Any intentional mismatch between preserved ancestry and current presentation is compatibility debt and must be registered, tested, and removed only through a versioned migration.

## Implemented now

- Spotlight live-read v1 adds an opt-in standalone Robinhood MCP/OAuth connection, normalized account/portfolio/position/quote reads, and verified append-only observation custody. `spotlight_observe` fetches deliberately; list/read use retained data without refresh. Windows DPAPI protects the separate connector credential file. `HUB_SPOTLIGHT_ENABLED=true` permits normalized financial data to enter local custody and the configured Resident provider; account identifiers and credentials stay outside that path. The installed room manifest/schema ancestry remains exact; bounded process-local connection ground is separately witnessed in Glass. Authentication and actual live verification are operational steps, not implied by installed code. Packet/replay/proposal/ring/execution hands remain capped. Owner: [`SPOTLIGHT_LIVE_READ_V1.md`](specs/SPOTLIGHT_LIVE_READ_V1.md).

- Progressive Corner readiness. The desktop and standalone browser hosts bind after the essential Source, Spine, current World/Hearth, and wake-orchestration gates rather than waiting for optional strict Forest verification. Active Forest verification runs in a cancellable worker; Forest custody, semantic Exhale, and traversal remain physically absent from Wake Service and Gateway until the exact proof succeeds. Health exposes bounded process-local shell, conversation, and Forest states plus stage timings. While active Forest readiness is pending or failed, Wake Service holds admission before any Source wake row is created; the Corner labels the pending state `Forest waking…`, shows bounded unavailability after failure, and unlocks only after exact proof and activation. Verification failure, concurrent Source advance, and shutdown cannot produce late or stale activation. Direct `createHub()` callers retain synchronous startup compatibility. Incremental ancestry checkpoints remain uninstalled pending a separately reviewed cryptographic format and measurement evidence.
- Semantic Forest ambient-feather selection and live Home Exhale. Once per accepted human turn with active Forest custody, after exact Home admission and before provider assembly, a local versioned embedding projection compares later-turn messages with Home terrain while excluding active Session Scroll sources and direct echoes. The first turn stays quiet. A deterministic quiet prior makes ordinary topical similarity clear stronger evidence while rare named anchors retain a lower first-feather threshold. Each bounded exact glint preserves the source ending and walks backward by complete sentences. The full selection is rooted as a non-respirable witness, while only a nonempty accepted packet enters Glass and receives exact presentation custody. A content-free two-turn departure footprint preserves happenedness without replay. Initial model/index warming produces witnessed silence rather than blocking the message.
- A plain shared `BUILDER_CHANNEL.md` correspondence surface. The Resident can read it from the Workshop repository and append an exact, stable-ID problem report through ordinary workbench custody; the Builder can reply with assessment or disposition. It is not automatic context, World authority, Forest memory, or a notification service.
- The standalone Workshop Continuity Seed is imported as an inert room-owned package surface. It provides a generation-1 SQLite operational store and bounded Cockpit projection for attributed objectives, facts, decisions, questions, Git observations, drift, and Kiln references. It is not yet mounted as a Resident tool or automatic wake source and stores no conversation, hidden reasoning, embeddings, or semantic Forest material.
- A forward-only Roots boundary for non-respiratory causal evidence. House Hearth packets remain rooted as before. Exact provider reasoning is stored once by content hash with causal edges to its Spine return, wake, and Session Scroll row. New assistant Scroll rows retain visible speech/action plus a typed pointer. DeepSeek thinking-mode requests carrying tools temporarily restore exact rooted reasoning for every retained assistant turn because the provider rejects omission; requests outside that exact shape project it out. Roots pointers never enter provider serialization, and reasoning remains absent from Forest, visible Scroll speech, and action authority.
- A forward-only Session Scroll trace epoch. Inherited history remains exact without invented ancestry; after `scroll_trace_boundary/v1`, every new Scroll row is atomically paired with an append-only source/gate/witness/destination/disposition manifest. Glass-ground, Forest-Exhale, Patch-Bay, and provisional-batch trace manifests remain visible future work.
- A bounded memory-only provisional intake collector before stream Scrub. It coalesces only within request/phase/channel, preserves channel order, flushes normal completions, discards paused failure/cancellation material, and prevents raw fragments from reaching persistence or display. Corner batches safe stream rendering to one animation frame.
- A forward-only Glass trace epoch. Every new cast item resolves to stable code-owned Glass, an exact ground receipt, a Session Scroll coordinate, or a Source event; Scrub disposition and provider ordinal close the path into the exact Spine request. Crossing, verified World, attention, continuity, and Patch Bay/schema mount receipts are append-only and atomically bound to the cast. Wake inspection exposes the complete packet walk.
- Stable Glass v4 carries Resident-reviewed trusted ground. It treats a settled action followed by new World ground as ordinary phase succession, limits installed affordances to material reach rather than understanding, preserves the distinction among record, inference, folded history, and unknown material, and asks the Resident to name genuine present friction without searching for faults. Current World presence explicitly identifies itself as ground for the current phase.
- Resident Presentation Clarity v1 is implemented. Dynamic crossing ground identifies the current wake and current session; exact prior-session inheritance declares author, excerpt standing, and non-recollection/non-truth standing; the Holster identifies Resident-authored inherited posture rather than host law; semantic Forest packets declare one enclosing associative prior-session seam; and Result Rack trail signs identify mechanical description rather than instruction. Stable Glass v4 and historical custody remain byte-exact, while unlabelled discoverable unknowns such as future Faun encounters retain truthful mystery.
- Verified World presence includes a bounded Spatial Horizon. It distinguishes current location, direct Hub containment, and installed directed routes: from the Garden it names the Hub to the south and its established rooms, while other places retain their truthful local relation and shortest known Workshop and Spotlight routes. The horizon grants no movement or room authority and disappears under World drift.
- One process-lived resident lifespan per server start, with Session Zero ancestry and a native two-breath `tend_hearth` handshake on the first human turn. Before the forced action, both situated ground and the capability schema explain that tending retrieves bounded, attributable continuity kept for the lifespan; retrieval does not endorse the return or prove memory. The literal Hearth projects the spatial affordance as available before tending, `✓ Tended this wake` after settlement, and `✓ Recently tended` on later turns in the lifespan. First tending settles once; the same House-local affordance remains callable afterward only to reread the exact retained packet without new settlement or inheritance.
- Exact Source Ledger, provider-presentation and provider-return Scrub receipts, hash-linked Spine custody, and optional verified Forest Home/Wild intake. Default runtime Spine custody is one independent JSONL hash chain per process-lived session; retained prior chains and the untouched legacy monolith remain available to aggregate verification without being loaded into the active Spine store. Home verification proves one exact atom per eligible user/resident utterance; Wild verification proves one exact span per eligible receipt-backed Workshop read/search result. An append-only Forest Intake Ledger records body-free source offers and `admitted`/`held`/terminal decision history, preserves attempted Home predecessor position, and exposes held/unresolved health counts. `forest:plan` / `forest:apply`, `forest:wild:plan` / `forest:wild:apply`, and `forest:intake:plan` / `forest:intake:apply` are separate confirmation-gated catch-up paths; retries are idempotent.
- Resident Journal planting. The universal host-owned `write_journal` affordance commits exact bounded Resident-authored text into append-only `home/journal` custody with session, wake, tool-call, provider-request, and Spine ancestry. Journal entries participate in semantic projection and Forest walking but not conversation `responds_to` chronology, World state, or automatic truth standing. Binder, Wild, Result Rack, Roots, and provisional material have no direct Journal admission path. Existing Forests receive only an empty additive companion until the Resident writes.
- Glass Casting v1 is the implemented provider presentation. On the first wake it supplies minimal physics and exact current ground, then carries the exact `tend_hearth` call and House Hearth return causally. The exact completed pair remains in the living Session Scroll until the ordinary retained-exchange boundary reaches it; it is not scrubbed immediately after the first answer. When it later leaves immediate attention, a deterministic Hearth trace retains the exact packet hash plus bounded exact source excerpts and Source/Forest bearings without replaying or summarizing the packet. After successful tending, later provider rounds also pin the occupied ten-slot Silver Bullet holster directly beneath stable Glass and above the deferred fold.
- Ordinary-dialogue rolling waterfall is installed in Glass. At the 80,000-byte default high-water mark it replaces the oldest eligible complete Human/Resident pairs until the projected crossing reaches the 64,000-byte low-water target, while retaining the newest four pairs, current incomplete turns, tool exchanges, stable ground, continuity, and mounted schemas exactly. Fold items carry bounded exact excerpts and source hashes as host-derived orientation; they do not become canonical memory or truth, and exact Source/Scroll/Forest custody is unchanged. The 120,000-byte hard ceiling remains fail-closed when non-foldable material alone is too large. Generated semantic summaries, hierarchical recapping, and direct fold walk-back remain deferred.
- Quiet Embodiment v1 is installed for ordinary human wakes. Stable Glass permits the Resident to move, look, or gather relevant material before speaking without narrating routine steps. A configurable four-action default horizon bounds simple movement and inspection inside the existing eight-round wake; verified local place bearings are possibilities rather than keyword routing or relevance claims. A spent horizon withdraws simple embodied schemas, same-batch excess refuses before World mutation, staying put remains complete, and autonomous wakes retain their separate bound. Existing World/Forest receipts and Corner Step Slips witness what actually occurred; no new action or perception authority is created.
- A persistent, event-projected World Graph with the Hub, Center, Workshop, Garden, House, Threshold, and literal House Hearth; per-lifespan location; five engageable Workshop fixtures; a stateful front door; a turnable Garden stone; visible non-traversable Forest/Road boundaries; a complete Ceiling catalog; and location-based Patch Bay mounting. New lifespans begin in the House before the Hearth; earlier lifespans retain their event-time origin. Fixture focus fits attention without changing World authority.
- Workshop fixture focus is one replaceable working focus. The whole room remains authoritative, while provider-facing schemas are fitted to the focused fixture on the next continuation; engaging another fixture switches directly without a disengage step. The stored v1 Workshop topology wording predates this fitting behavior and remains hash ancestry; current presence and action descriptions state the live law.
- The first `tend_hearth` return is deterministic bounded Markdown carrying the Ember lineage orientation, six adopted Silver Bullets in a ten-slot rack with four invisible blanks, and exact bounded prior-session excerpts with Forest pointers when available. The fourth distinguishes an absent line from a lie, the fifth makes inquiry land rather than become refuge, and the sixth rejects inaction as a false way to preserve the Marble while retaining consequence and real permission boundaries.
- Bounded repository exploration and mutation, local Git operations, work briefs, timers, approvals, and deterministic Corner Step Slips. Search, regex search, and glob now traverse eligible files incrementally, skip declared dependency/build/cache directories, and return exact partial results with examined/skipped counts and an honest boundary instead of refusing an ordinary large scope before evaluation. Reads disclose total lines and the next exact range; mounted schemas advertise configured limits. The Workshop catalog includes a bounded mechanical repository overview and distinguishes installed tools from those immediately fitted.
- Result Rack custody for ordinary Gateway success, refusal, pending approval, and approval-completion crossings. Exact machine-result bytes/manifests are retained in SQLite and resident tool returns use deterministic `result_rack_projection_v1` host-return Scrub projections. Asynchronous recipe completion is also captured.
- Truthful extent and Workshop document reading are installed. Bounded document outline/read results carry validated source revision, requested/examined/presented/missing ranges, and host-derived same-session coverage from append-only World action receipts. Whole modest documents use a separate bounded Result Rack projection policy without widening ordinary command, diff, or test output. `forest:check` provides a fast read-only jurisdiction/frontier census; `forest:verify` remains the forensic ancestry authority.
- Recoverable Result Exhale. A completed tool exchange may leave immediate attention only when every result has a verified same-lifespan Result Rack projection. Forest walk returns now receive ordinary Rack custody as well as their exact traversal receipts, allowing earlier completed exchanges to waterfall during a long active wake while the configured newest exchanges and every incomplete exchange remain as the exact causal tail. Glass carries the full bounded pointer-only trail-sign set once per causal human wake; later tool continuations carry one deterministic exact-pointer continuation marker instead of replaying every full sign. The universal host-owned `reopen_result` affordance deliberately reopens one exact stored projection without World action, room, approval, execution, or mutation authority. Every visible sign, continuation marker, and reopening is retained in append-only Roots exposure custody with exact Glass/Scrub/Spine ancestry and is explicitly non-respirable and non-Forest-eligible.
- Semantic Forest Exhale is live for Home after the first human turn. The active `ambient_vector_feathers/v2` selector uses a rebuildable local BGE generation, bounded semantic neighborhood, a deterministic quiet prior with rare-anchor relief, graduated evidence floors, diversity and direct-echo suppression, and at most three end-anchored exact preview feathers. The first turn records quiet without querying. Every decision or silence is retained as a Roots selection witness; each nonempty packet shown to the Resident receives separate exact Glass/Scrub/Spine exposure custody. For two later human turns, a content-free footprint preserves only that a breath crossed and departed; it never replays or respirates the feather. During an active walk, a passing feather is a wake-scoped distant bearing that may be chosen through `walk_toward`; unchosen landings expire. Higher-canopy dolls and the Faun remain uninstalled.
- Action rounds are quietly legible inside the wake through a general action horizon rather than ambient Workshop machinery. Each action-capable continuation states how many further rounds may be used, and the configured boundary reserves one schema-free final response. A provider tool call emitted despite that absent mount is refused and the wake fails honestly; the host never fabricates completion.
- Self-directed rest and same-seat autonomous return are installed. After tending the Hearth, the Resident may call `rest_for` for 60 seconds through 24 hours; one pending append-only plan per lifespan retains its life/context generation, loose intention, exact World/Forest footing, rest time, requested duration, and due time. Dispatch records actual wake time, elapsed interval, and lateness separately and presents those exact facts inside the wake. On return the ordinary Session Scroll, Glass, World, Forest, Scrub, and Spine paths continue without a fabricated user message or context-reset claim. Autonomous wakes receive 24 reversible/read-only rounds by default, with both presented schemas and dispatch enforcing reduced authority. A local manual wake-now endpoint proves the crossing. Ordinary Hearth-origin scheduling remains a distinct crossing under its own law.
- Opt-in ordinary Hearth-origin waking is installed and disabled by default. `HUB_HEARTH_WAKE_INTERVAL_SECONDS` accepts `0` (off) or 60 seconds through seven days. A due tick waits for readiness and active work, yields to any pending bench promise, closes the prior lifespan, opens a fresh lifespan at `place.house`, and performs the existing two-breath Hearth ritual without fabricating a user utterance. The wake receives the same reduced autonomous tools and 24-round horizon; live Spotlight observation and other consequential hands remain absent. Exact schedule/due/wake/lateness facts receive host-event and append-only origin custody. Restart begins a new process-local interval and does not fabricate missed offline ticks. A local empty-body manual endpoint proves the crossing without enabling recurrence. Owner: [`HEARTH_ORIGIN_WAKES_V1.md`](specs/HEARTH_ORIGIN_WAKES_V1.md).
- Provider reasoning is explicitly fitted rather than left to DeepSeek's high-effort default. The installed Marble postures are `light` (normally thinking enabled with provider effort `low`) for Garden phases and continuations after simple embodied actions, and `attentive` (thinking enabled with provider effort `high`) elsewhere. Forced Hearth orientation is a light deterministic exception: DeepSeek thinking is disabled so its required `tool_choice` remains provider-valid, then the first response after receiving the Hearth packet is attentive/high. Every request witnesses both posture and exact provider control. `max` is deliberately uninstalled; posture changes no model identity, capability, custody, or authority.
- Provider attention fitting: a persisted per-phase attention meter, health projection, fitted resident tool schemas keyed by the engaged fixture, and declared whole-message omission of older completed tool-call/result exchanges. Current and configured recent exchanges remain; every omission is recorded by provider-presentation Scrub with a disclosure while the exact original history remains in host custody. World tool authority remains unchanged.
- The workbench fits sandbox diff preview beside sandbox promotion, so the Resident is never offered the confirmation path without its exact candidate view.
- Append-only approval custody. A pending confirm-class call records a pending approval receipt linked to its original action receipt; confirmed, rejected, and restart-cancelled decisions create distinct completion action and approval receipts. Promotion confirmation is therefore separate from the initial pending result.
- Live-mode named recipes run in one persistent job-scoped Docker Sandbox Bay backed by a disposable Git worktree. The canonical checkout is not mounted read-write, container networking is `none`, the child environment is minimal, resource caps are declared, and backend failure refuses without host fallback.
- `workshop_sandbox_diff` and always-confirm `workshop_sandbox_promote`. Promotion is plan- and patch-hash bound, requires a clean and unchanged canonical checkout, refuses symlink/submodule and Hub control paths, applies only through the host-owned crossing, records completion custody, and resets the sandbox job after success.
- The complete Workshop implementation is source-coherent under `src/places/hub/workshop/`: declaration, repository adapter, Git, recipes, Sandbox Bay, promotion, and protected-path law. Historical World and path-law imports are thin compatibility doors. This is not yet dynamic room discovery or automatic installation; World, Ceiling, Gateway, Scrub, and custody crossings remain explicit host wiring.
- `room-installation.v1` validates inert room installation requests. Workshop carries the first standing `room.json`. A deterministic host-owned `room-installation-witness.v1` hashes the exact manifest and verifies Workshop placement, parent containment, entrance, fixtures, Ceiling, mounting, schemas, Gateway handlers, approval classes, sockets, and custody; bounded Builder World inspection exposes it and names every loose wire. The manifest cannot self-install, self-authorize, create its door, supply its sockets, or install custody. Generic discovery, dynamic admission, transactional wiring, disable/removal, package provenance, and marketplace machinery are not implemented.
- A forward-only `room-installation-receipt.v1` ledger binds exact verified room witnesses to exact World events, manifest hashes, host bindings, admission statements, and installation time. Workshop's original `inherited_pre_boundary` receipt remains immutable; Workshop `1.1.0` is admitted by a strict `room.installation.revised/v1` World event and forward receipt bound to that ancestry. Startup is inspection-only and fails closed on drift. The backup-confirmed `room:migrate-workshop` command is the sole bounded revision path; this is not generic room lifecycle machinery.
- The Corner has a read-only expandable Marble Inspector backed by the exact installation witness. Opening World inspection widens the working surface while keeping conversation present; the Inspector navigates overview, topology, each Workshop affordance, sockets, custody, hashes, and loose-wire state, with raw World data retained as a separate tab. It is the first expandable task surface, not yet a general browser/chart/notepad panel framework.
- The Result Rack implementation is split across `src/result-rack/`, with `src/world/results.js` retained as a compatibility facade. Runtime code imports Result Rack and attention owners directly, and the World Gateway dispatches through a complete registry checked against the installed Ceiling catalog.
- `WakeService` owns wake orchestration. DeepSeek responses stream over provider SSE; bytes admitted under the 8 MiB default `HUB_PROVIDER_MAX_RETURN_BYTES` ceiling are retained exactly in the Spine and independently assembled by provider-return Scrub before terminal use. Oversized returns seal the exact admitted prefix as incomplete custody and fail closed.
- Durable append-only `wake_stream_events` custody and a bounded process event bus. Safe provisional thinking/content/tool-name deltas, receipt-backed tool/approval/result cards, and terminal events are persisted before broadcast. Tool argument fragments are withheld, and credential-shaped provisional channels are suppressed without changing final provider custody. `GET /api/events` exposes same-origin named SSE events; `GET /api/events/history` is the recovery authority when replay requires resynchronization. Corner keeps polling as a fallback.
- Live Corner thinking, phase-bound speech, tool-call, approval, diff, and result views derived from provisional provider envelopes or committed host receipts/Result Rack projections. Exact mid-call speech is sealed as a durable **Resident · en route** step and remains ordered across later tool phases and after completion; it does not become a canonical utterance or Forest atom. Only the terminal scrubbed message enters conversation history and ordinary Forest admission.
- An Electron 43.2.0 Corner shell launched by `The Hub.exe` on Windows or `npm run desktop`: single instance, owned loopback Hub lifecycle, secure preload/IPC boundary, 96x96 compact and 980x680 expanded geometry at a 24-pixel display margin, tray expand/collapse/quit, ordinary-close hiding, configurable normal always-on-top, and renderer parity with the browser. The Windows launcher opens no console and surfaces the recorded startup failure when Electron exits unsuccessfully.
- Corner Place-First v1 is implemented in the shared browser/Electron renderer. It retains the compact presence, active-lifespan conversation, live causal steps, approvals, wake inspection, and Marble Inspector while leading the expanded surface with verified current place, a room-aware but non-authoritative atmosphere, safer long-response typography, a read-only projection of installed fixtures and hands within reach, and an explicit `Behind the wall` Builder layer. World projection is refreshed only when location or engagement changes. The revision adds no movement, room, tool, discovery, or observation authority.

## Specification precedence

Later adopted slices supersede only the named parts of earlier slices; remaining custody and constitutional laws stay in force.

| Current authority | Supersedes |
| --- | --- |
| `SPOTLIGHT_LIVE_READ_V1.md` | The shell's unconditional observation/list/read caps and absent standalone transport/custody. Installed room/schema/witness ancestry remains exact; financial, evidence and replay caps remain in force. |
| `GLASS_CASTING_V1.md` | The active provider-presentation shape and the Longshore Current continuity-name / wake-blessing roles in Wake Ritual, Session/Hearth, and Hearth Scroll. Historical records and custody remain unchanged. |
| `SESSION_HEARTH_V1.md` and `HEARTH_SCROLL_V2.md` | First Breath / Orientation Receipt / Wake Ritual presentation timing and the original large Hearth return |
| `WORLD_GRAPH_WORKSHOP_V1.md` | Earlier claims that no rooms or movement exist |
| `THRESHOLD_HOUSE_GARDEN_V1.md` | The Center/Workshop-only topology and door-only movement surface; B1 adds the exact versioned exterior shell and passage laws while deferring Threshold wake chronology and writing law. |
| `WORKSHOP_TOOL_CEILING_V1.md` | Station-to-tool mounting and the read-only Workshop capability ceiling |
| `WORKSHOP_FIXTURES_V1.md` | Living Spec Table and Control Panel stations; fixture engagement replaces station engagement |
| `WORKSHOP_HEARTBEAT_V1.md` | Leave-room recipe cancellation; kiln and timer are house-bound within the lifespan |
| `CEILING_PATCH_BAY_V1.md` | Ad hoc tool-list projection; the Ceiling catalog and room profiles own mounting |
| `CORNER_STEP_SLIPS_V1.md` | A single undifferentiated active-wake gap |
| `WORKSHOP_SANDBOX_BAY_V1.md` | Direct-host recipe execution in live mode and promotion without an isolation boundary |
| `WORKSHOP_RESULT_RACK_V1.md` | Unfitted ordinary host returns and unreceipted removal of old completed tool exchanges |
| `WAKE_STREAM_V1.md` | Polling-only active-wake presentation and non-streaming provider-return assumptions |
| `CORNER_DESKTOP_V1.md` | The web-only wrapper boundary in `CORNER_SURFACE.md` section 2 |
| `HOUSE_HEARTH_WAKE_V1.md` | B1 Center-start law, passive first-wake Glass inheritance, and the prior Hearth presentation |
| `RESPONSIVE_PRESENCE_V1.md` | Global implicit high-effort provider reasoning and the post-settlement disappearance of the House Hearth affordance |

The Glass row describes the current runtime presentation. Session/Hearth v1 and Hearth Scroll v2 remain implementation ancestry and custody law where Glass does not supersede them. `WORKSHOP_CONTROL_PANEL_V1.md` and `WORKSHOP_STATIONS_V1.md` likewise remain implementation ancestry. Use them only where a later Workshop specification has not superseded their claims.

## Implemented core, broader design still partial

- [`THRESHOLD_HOUSE_GARDEN_V1.md`](specs/THRESHOLD_HOUSE_GARDEN_V1.md): B1 is implemented and verified. An exact append-only topology extension installs the Hub container, Garden, House, Threshold, Center/Garden opening, stateful Garden/House front door, House/Threshold passage, visible non-traversable Forest/Road boundaries, globally persistent door and stone state, a turnable Garden stone, and inert Center marker/House window. Journal-bearing A2 stores require the explicit read-only-by-default, backup-confirmed `world:migrate-b1` path; startup never upgrades or repairs them. Threshold wake chronology, marker/window writing law, inventory, and boundary crossing remain deferred B2 work.
- [`WORKSHOP_SANDBOX_BAY_V1.md`](specs/WORKSHOP_SANDBOX_BAY_V1.md): live Docker execution, disposable worktree custody, no host fallback, diff inspection, and hash-bound approval-gated promotion are implemented. Durable backend-neutral lifecycle events, immutable brief-revision binding, restart recovery of an external sandbox, independent repository metadata, and broad artifact/resource telemetry remain design work.
- [`WORKSHOP_RESULT_RACK_V1.md`](specs/WORKSHOP_RESULT_RACK_V1.md): append-only jobs/status/output/artifact/projection rows, exact pointers, deterministic bounded projections, ordinary Gateway integration, async recipe completion, provider attention measurement, and declared old-tool-exchange omissions are implemented. General sandbox file-artifact collection, Rack list/read tools, retention/disposal policy, parser-derived observations, and complete promotion-package artifact custody are not.

Result Rack currently retains exact machine-result JSON and output chunks plus their manifests. It must not be read as a claim that arbitrary files produced inside a sandbox are collected or durably retained.

## Workshop trust and safety boundary

Workshop paths remain relative to the configured root and refuse traversal, symlinks, `.git`, `.runtime`, environment files, and credential-like paths. The resident cannot supply an arbitrary shell command. Network Git and destructive/rewriting Git operations are not installed.

Live mode requires `HUB_SANDBOX_BACKEND=docker`. The configured `HUB_SANDBOX_IMAGE` must already be present locally: jobs run with network disabled and the Hub does not pull an image as an execution fallback. If Docker, the image, worktree provisioning, or a required control is unavailable, recipe invocation refuses; it never runs that live recipe in `HUB_WORKSHOP_ROOT`.

Fake mode is different by design: it retains the legacy direct-host RecipeRunner for tests and demonstration. That runner executes trusted workspace code with the Hub process's filesystem permissions and is not an OS sandbox. `HUB_SANDBOX_BACKEND=host-test` is accepted only with fake configuration, but `createHub` does not instantiate the HostTestSandboxBackend. Do not use fake recipe execution for untrusted repositories.

`HUB_APPROVAL_MODE=auto` is a fake-mode test convenience. Live mode keeps confirmation-class actions gated, and sandbox promotion remains confirmation-gated even in auto approval mode. Pending approvals belong to the lifespan that created them and are cancelled rather than confirmable after restart. A kiln left `running` by process termination is reconciled as cancelled.

For a confirm-class call, the initial action receipt may be `committed` because creation of the pending approval was the immediate committed result. The later decision and mutation have their own append-only action and approval receipts linked to the pending phase.

## Adopted design, not implemented
- [`SECURITY_PRIVACY_CUSTODY_V1.md`](specs/SECURITY_PRIVACY_CUSTODY_V1.md): adopted security architecture and readiness gates for classification, sealed-pointer custody, encryption/key lifecycle, bounded sensitive attention, direct Vault intake, remote devices, migrations, destruction, and independent review. No application-level encryption at rest, authentication, secret manager, sealed Vault blob store, remote device admission, cryptographic erasure, or security certification is implemented today.
- [`HOUSE_GRAMMAR_V1.md`](specs/HOUSE_GRAMMAR_V1.md): active constitutional vocabulary, including the Recursive Forest Law. Every Forest repeats Home, Wild, and Mycelium at its own scale; a whole Forest may participate as one member at a wider scale without rewriting its interior jurisdictions or laundering leaf authority. Recursive composition is distinct from the Forest's exact containment dolls. This adopts grammar only, not recursive substrates, traversal frames, portals, Mycelium storage, or a `place.forest` projection runtime.
- [`FOREST_PATHS_ROLLING_FOLD_V1.md`](specs/FOREST_PATHS_ROLLING_FOLD_V1.md): directional walking, the Forest-place revision, and recoverable same-wake Forest exchange waterfall are implemented. `place.forest` projects `forest.resident`; physical Garden travel, shared inquiry, and red-thread recovery retain distinct registers. New v3 junctions distinguish the exact walked edge behind, structural conversation exits, and a semantic fan whose straight bearing is direct resonance while left/right are jointly selected opposing poles with carried handedness. Entry casting excludes the tether utterance so the present question cannot masquerade as its own answer. Current footing is explicitly post-action confirmation rather than prediction. In-Forest ambient feathers are explicitly passing, wake-scoped distant bearings; local terrain is excluded, an unchosen bird expires, and choosing one through `walk_toward` creates an ordinary durable semantic edge with feather provenance. Chosen semantic edges persist separately and accumulate wear. Physical visits may `leave_forest` from any trail length to the treeline; projected visits `turn_around` to their retained present. The first v2 expedition remains exact `legacy_flat/v2` ancestry. General nesting dolls, the Faun, recursive frames, broader utterance folding, and Write/Leave synthesis remain unimplemented.
- [`FOREST_JOURNAL_V1.md`](specs/FOREST_JOURNAL_V1.md): deliberate exact Resident Journal planting is implemented as walkable `home/journal`, separate from ordinary utterance chronology. The adopted Home--Stream--Wild bridge, Bear/Faun entry question, traversable Wild, raw-packet containment, and required homeward synthesis remain explicitly deferred.
- [`WORLD_EVENT_PROJECTION_DRIFT_V1.md`](specs/WORLD_EVENT_PROJECTION_DRIFT_V1.md): A1 and A2 are implemented and verified. The hash-linked World journal and deterministic projector now cover physical state plus kiln runtime, timers, retained brief revisions, approvals, and exact event-linked custody. Legacy physical/operational boundaries are explicit; A1-only journal upgrades are backup-gated and never run by startup. External approval effects enter durable non-retryable `applying` state before execution. House/Garden work may now build on this pipe.
- [`FOREST_EXHALE_V2.md`](specs/FOREST_EXHALE_V2.md): recoverable Result trail signs, deliberate bounded reopening, Roots exposure custody, and Home-only ambient vector feathers are implemented. Greeting turns stay quiet; direct echoes are rejected; later nonempty packets cross as exact, non-authoritative Resident attention with complete Glass/Scrub/Spine custody.
- [`BINDER_WINDOW_V1.md`](specs/BINDER_WINDOW_V1.md), [`SPOTLIGHT_OBSERVATORY_V1.md`](specs/SPOTLIGHT_OBSERVATORY_V1.md), and [`SPOTLIGHT_LIVE_READ_V1.md`](specs/SPOTLIGHT_LIVE_READ_V1.md): the passive Binder Window and enterable five-fixture Spotlight observatory are installed. Binder inspection uses its frozen local snapshot. Spotlight movement mounts the room profile and never requests data. Its optional standalone Robinhood connection activates deliberate observation and retained list/read through the room-owned store. Missing configuration/authentication fails honestly; Codex's session is not inherited. Evidence/replay/proposal/ring/execution activation, brokerage actions, automatic delivery and general sealed financial custody remain uninstalled.
- `VAULT_V1.md`: the Vault room, door, document graph, wings, bin/slot crossing, principle documents, and Forest-to-Vault pointers.

## Current streaming and desktop boundaries

- Durable wake events use monotonic, append-only, hash-linked custody and persist before process broadcast. The process replay buffer is an optimization. Its `resync_required` envelope is synthesized non-durable transport control, not a journal row; Corner preserves its last contiguous cursor and pages the durable history endpoint before reconnecting.
- Provider deltas are provisional (`authority=provider_provisional`, `committed=false`). Host lifecycle/tool/card events are receipt-derived. The terminal scrubbed assistant message is the only provider message admitted to canonical history and downstream authority.
- Disconnecting EventSource does not cancel, pause, or backpressure an active wake. User cancellation is not implemented, and the 400 ms persisted-slip poll remains the renderer fallback. Process shutdown does abort the active provider crossing, allows 250 ms for partial raw/outcome custody, then gates late callbacks before closing stores. Tool and World execution outside that provider window remains awaited.
- DeepSeek SSE bytes are parsed as they arrive, but the Spine appends one terminal raw-return frame rather than one record per network chunk. Normal and interrupted crossings retain the complete admitted bytes; an oversized crossing retains only the exact bounded prefix admitted before the offending chunk and records incomplete capture.
- Source Ledger writes use a bounded SQLite busy wait (`HUB_SQLITE_BUSY_TIMEOUT_MS`, 5 seconds by default), allowing transient external write contention to clear without terminating a wake. Contention beyond the bound still fails explicitly and does not bypass append-before-broadcast custody.
- The desktop shell has automated geometry, lifecycle, and security coverage. Native GUI/tray smoke verification remains pending, and no installer, signing, update, or packaging pipeline exists yet.
- Corner does not currently provide general file/artifact opening, editor navigation, or source highlighting. Result and diff cards are bounded textual projections with exact custody pointers.

## Deferred

The partial Sandbox Bay and Result Rack items named above; general sealed-pointer custody, encryption/key lifecycle, Hub user authentication, security migrations, cryptographic erasure, and independent review; the unimplemented nesting-doll, rolling-fold, broader path-wear, Faun, recursive-frame, and Write/Leave portions of Forest Paths; Wild selection and automatic counterweight judgment; Vault runtime; remaining Threshold/House/Garden writing laws; Backpack/threads; automatic Binder delivery and sealed provider-disclosure policy; Box retained/active observation crossings; Pipes atlas/live-knock crossings; other Forest/Road crossings; Spotlight evidence/replay/proposal/ring/execution activation and automatic refresh; reset UI; free-form shell; companion/delegation bridges; multi-root workspaces; user cancellation; streaming backpressure; per-chunk Spine custody; desktop packaging/installers; general file/artifact opening and highlighting; external publishing, deployment, and network Git operations.
