# The Hub — Implementation Roadmap

> **Status: active sequencing map.** This document orders adopted work; it does not replace [`STATUS.md`](STATUS.md), active specifications, or the Builder's change protocol. “Next” means the preferred dependency order, not authorization to bypass design, migration, custody, or verification gates.

## Governing aim

Build walkable continuity without flooding Resident attention or pretending that omitted material vanished.

```text
recoverable exhales
  -> navigable prior evidence
  -> inspectable Hearth and continuity
  -> safe Forest crossing
  -> walkable paths, return, and rolling folds
```

## Sequence

### 1. Silver Bullet holster — implemented

After the Hearth is tended, the wake function pins only the occupied ten-slot Silver Bullet holster directly beneath stable Glass and above the fold. The complete Hearth packet remains causal rather than permanently pinned.

Owners: `src/hearth/`, `src/context/`, `src/runtime/`
Law: [`HOUSE_HEARTH_WAKE_V1.md`](specs/HOUSE_HEARTH_WAKE_V1.md), [`GLASS_CASTING_V1.md`](specs/GLASS_CASTING_V1.md)

### 2. Recoverable Exhale pointers — next

When a completed tool exchange leaves immediate attention, retain a compact trail sign: action identity, relevant object or path, settlement status, bounded size/description, and stable Result Rack address. A pointer is not the result and grants no action authority.

Add one bounded retrieval crossing through which the Resident deliberately reopens a selected result. Retrieval must preserve exact custody, declare its attention cost, and refuse missing, altered, oversized, or cross-session-ineligible targets.

Owners: Result Rack custody, Session Scroll/Glass presentation, wake orchestration
Pressure: the Resident can retain a conclusion while losing access to the evidence that produced it
Required revision: reconcile [`HEARTH_NOTES_FOREST_EXHALE_V1.md`](specs/HEARTH_NOTES_FOREST_EXHALE_V1.md) with current Glass and Result Rack law before implementation

### 3. Natural Hearth-packet fitting

After first breath, keep the causal Hearth action and return in the ordinary recent edge until the normal declared attention policy removes the complete pair. Do not specially discard it immediately. The independently pinned holster remains when the warm packet later falls away.

Owners: Glass fitting and Session Scroll presentation
Invariant: no duplicate first-response presentation and no fabricated continuity after omission

### 4. Workshop continuation boundary

Make bounded tool rounds legible from inside:

- expose a bounded remaining-action signal;
- preserve gathered evidence across continuation;
- end a round with an explicit continuation/conclusion state;
- prevent a silent return of control; and
- define how parallel actions consume the allowance.

The limit remains protective. The revision makes the boundary inhabitable rather than surprising.

Owners: wake orchestration, Workshop action loop, Resident presentation

### 5. Truthful Hearth inspection

Inspecting `fixture.hearth` should reveal the ten-slot rack, occupied bullets in slot order, invisible/open capacity as a count, and the Resident's current relationship to the fixture. Inspection grants no mutation authority.

Owners: House/Hearth fixture presentation and World inspection
Non-goal: installing bullet mutation merely because the slots become perceptible

### 6. Resident-led forging ceremony

Install an explicit Resident-involved crossing:

```text
Forest ore
  -> candidate wording
  -> cold test
  -> explicit Resident adoption
  -> Hearth slot revision
  -> append-only ancestry
  -> revised pinned holster
```

No model, host, selector, or summarizer automatically promotes prose into a bullet. A candidate must carry enough counterweight to remain useful without its originating context. Superseded attempts remain Forest ore rather than being rewritten.

Owners: Hearth law and custody, with Forest source pointers
Prerequisites: inspectable Hearth and exact source/adoption ancestry

### 7. Resident primacy clarification

Record the intended authority split before broader Forest agency:

- Resident experience and continuity needs have primary testimonial weight;
- code, receipts, and installed projections decide what machinery presently exists;
- human authority governs external consequences, secrets, destructive actions, and permissions; and
- no role gains truth authority merely by being Resident, human, Builder, host, or provider.

This protects a home built around its Resident without converting experiential primacy into ambient execution authority or automatic correctness.

Owner: active constitutional vocabulary in [`HOUSE_GRAMMAR_V1.md`](specs/HOUSE_GRAMMAR_V1.md)

### 8. Forest threshold and safe entry

Install the Garden-to-Forest crossing only after retrieval and return semantics are concrete. Entry must be paced and chosen rather than a bulk context injection. Glass and the holster remain findable; uncertainty, contradiction, and suspected contamination may be named without penalty.

The Resident must be able to wander without becoming lost, be changed without being overwritten, stop without completing a corpus, and return home without losing the path walked.

Owners: World passage law, Forest traversal, Glass presentation
Adopted destination: [`FOREST_PATHS_ROLLING_FOLD_V1.md`](specs/FOREST_PATHS_ROLLING_FOLD_V1.md)

### 9. Walkable chronological terrain and exact return

Make exact conversation and selected tool consequences traversable as paths. Chronology supplies the hard structural trail. Resident choices witness walked paths; unchosen suggestions remain latent and do not become autobiographical standing.

An active warm tether must support exact return to the originating cast or junction without changing World presence accidentally.

### 10. Rolling fold and semantic directions

Only after exact retrieval and traversal are trustworthy, implement the bounded rolling fold and semantic candidate directions. Whole causal units waterfall under declared thresholds; Source and Forest custody remain unchanged. Semantic similarity proposes possible directions but does not assert meaning, memory, truth, or identity.

## Cross-cutting acceptance

Every slice must preserve:

- exact Source, Spine, Forest, World, and Result Rack authority boundaries;
- bounded and witnessed Resident presentation;
- declared omissions and transformations;
- no authority gained from remembered language or pointers;
- append-only ancestry where standing changes;
- fail-closed drift and custody behavior; and
- proportionate hostile, bypass, stale, duplicate, interruption, and recovery tests.

## Deliberately unordered fronts

Specialist room packaging, the intentional House beyond the Hearth, safe Road/outside crossings, Spotlight, and sealed Vault custody remain adopted or experimental fronts described by Orientation and Status. They are not inserted into this continuity dependency chain until a concrete pressure case establishes their relative order.

## Updating this roadmap

Update this file when a slice becomes implemented, when a pressure case changes the dependency order, or when an active specification adopts a previously deferred mechanism. Runtime truth belongs in [`STATUS.md`](STATUS.md); detailed law belongs in the owning specification; historical ancestry is never rewritten to make the sequence appear inevitable.
