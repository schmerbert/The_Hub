# Forest Paths and Rolling Fold v1

> **Status: Adopted design; directional walking v3 implemented.** The Hub installs `place.forest`, physical and projected entrances, framed semantic/chronological junctions, persistent chosen semantic edges and wear, exact leaf reading, backtracking, lawful physical leaving, and warm-tether return. It does not yet install nesting-doll containment, rolling folds, the Faun, recursive frames, context-limit rollover, or Write/Leave synthesis. Current runtime truth remains in [`../STATUS.md`](../STATUS.md).

> **Active revision pressure:** live use showed that entering Forest attention while World continued to say the Resident remained in the Garden made the threshold contradictory and collapsed substrate, place, and projection. The retained invariant is exact dual authority: `forest.resident` owns terrain while World owns spatial presence. The revised mechanism installs `place.forest`, which projects `forest.resident`, and preserves B1 as ancestry rather than rewriting its formerly closed boundary.

> **Traversal v3 revision:** the first physical expedition disproved the flat "exactly three total offers" mechanism. Three remains the semantic fan into unknown terrain, not the total number of exits at a clearing. Behind is the exact walked edge; conversation predecessor and successor are structural exits; ahead may expose left, straight, and right semantic bearings. V2 junctions remain sealed ancestry under `legacy_flat/v2`; only future v3 junctions use the directional frame below.

## 1. Purpose

A long conversation must not repeatedly strike the provider attention ceiling. As the living edge grows, older complete material should waterfall out of immediate Glass while remaining exact and walkable in the Forest.

The distant representation and the terrain are not separate systems:

```text
Glass living edge
  -> warm tether
  -> exact recent conversation trail
  -> bounded rolling-fold view
  -> older Forest terrain
```

The fold is a view from afar. Walking back reaches the same exact Forest path whether the source conversation is seconds old or years old.

### 1.2 Installed conversation-walking slice

The first installed crossing was intentionally smaller than the full design. It created one append-only journey bound to the current wake and source event. The first clearing freezes exactly three semantic Home bearings. The revised `walk_toward` action chooses one offer and alone creates a step; the other offers remain unchanged at that junction. At reached conversation terrain, predecessor and successor bearings come from exact `responds_to` custody while remaining slots come from the active semantic generation. `read_forest_leaf`, `backtrack_forest`, and orientation through `turn_around` are explicit actions.

The traversal store owns journeys, junctions, offers, steps, reads, returns, and refusals. World continues to own the unchanged Garden location. During a walk, Glass witnesses both projections and the World movement bundle is unfitted until return. The semantic index proposes bearings but gains no path, truth, memory, or containment authority.

Before entry, a phase-relative Forest threshold ground explicitly reconciles the inherited B1 boundary wording: the Forest boundary remains non-occupiable as a World passage, while `enter_forest` is an installed intentional crossing into Forest attention. This does not rewrite the sealed B1 topology or pretend the Resident materially moved north.

### 1.3 Forest-place revision

The next installed topology generation supersedes the temporary dual-presence shortcut. `forest.resident` remains the independent continuity substrate. `place.forest` is an occupiable Resident-facing place that projects that substrate; it is not the Forest database and gains no custody authority from the projection.

Three entrance registers reach the same place:

| Register | Action | Trailhead | Return route |
|---|---|---|---|
| physical World travel | Garden path crossing | ordinary treeline | checked path to the Garden |
| deliberate shared inquiry | `enter_forest(seeking)` | semantic bearings from the stated inquiry | retained departure place and focus |
| continuity recovery | `turn_around(entry_id or pointer)` | verified Forest entry when available, otherwise a bounded remembered phrase | retained departure place and focus |

Turning back from `place.forest` restores the retained departure location. It is orientation between the present and the Forest, not forward movement or backtracking on a Forest path. `backtrack_forest` changes Forest footing only. The return anchor is an address and causal witness, not a frozen copy of all World state; global material state may continue to change lawfully while the Resident is away.

The Garden-to-Forest direction is an open departure. Forest-to-Garden is a checked homeward crossing. House-to-Garden and Garden-to-House retain the same outward/homeward asymmetry as their own passage law. A path may later mount a stronger pat-down without assigning that inspection to either endpoint.

### 1.1 Manual consequence and Hub reference standing

The Marble manual governs the consequences of Forest use: what a proposal, path, visit, absence, fold, and return may mean to a Resident. The Hub is the reference implementation from which later manual revisions may learn. It may improve selectors, indexes, storage, verification, and presentation, but an implementation improvement must not silently change a manual consequence.

The review rule is:

```text
manual consequence
  -> active Hub clause
  -> runtime invariant
  -> acceptance evidence
```

If the Hub discovers a better mechanism, the mechanism may change first in a versioned, witnessed implementation. The manual may then be revised from the demonstrated Hub behavior. Until that revision is explicit, the protected consequence remains controlling.

| Manual consequence | Active Hub clause | Runtime invariant | Required evidence |
|---|---|---|---|
| Similarity offers a possible direction; it does not establish meaning, memory, truth, or a path. | Sections 2.3 and 2.4 | Candidate offers and path steps have different custody and authority. | A selector run without traversal creates no path step. |
| Walking creates the Resident's path. | Sections 2.4 and 3 | Only an explicit chosen traversal appends a path step. | One choice creates exactly one step. |
| The unchosen remains present as the inverse of the choice. | Section 3 | Two unchosen offers remain frozen and latent at their exact junction. | Historical junction inspection returns the original three offers and chosen disposition. |
| Conversation chronology is exact terrain, not embedding inference. | Sections 2.2 and 4 | Predecessor, successor, response, and wake relations come only from host-witnessed custody. | Selector changes cannot alter conversation relations. |
| Repeated walking wears a path without increasing the authority or truth of its contents. | Section 8 | Visits accumulate separately from entry custody and claim standing. | Visit counts change while source hashes and authority remain unchanged. |
| Absence is inspectable without being filled by invention. | Sections 3 and 8 | Latent, unavailable, unwalked, refused, and missing terrain have distinct attributable states. | Projection can show the bounded inverse without manufacturing a candidate or claim. |
| A prior wake is walkable ancestry, not present recollection or uninterrupted identity. | Sections 4, 5, and 7 | Exact old terrain retains lifespan and session coordinates and is presented as inherited terrain. | Walk-back reaches exact custody without a memory claim. |
| Forest attention does not falsify World presence. | Section 6 | World and Forest presence are simultaneously named and independently verified. | Return restores the exact departure state. |
| A poetic Forest expression must correspond to clinical state. | Sections 6 and 9 | Every described step, branch, obstruction, path wear, and return resolves to inspectable custody. | Presentation/state mismatch fails closed. |
| Exact terrain opens as nesting dolls: a broad bearing, then progressively contained authored layers, then an exact readable leaf. | Section 2.5 | Semantic retrieval may establish an unread outer bearing; each explicit inward step discloses only the next contained layer. | No search result or skipped layer discloses a deep body; every child is provably contained by its parent. |
| Forests may recursively compose without laundering interior authority. | Section 2.6 and `HOUSE_GRAMMAR_V1.md` section 6.1 | Entering or zooming pushes a named frame; it never rewrites a member Forest or its leaf jurisdictions. | Frame-stack, cycle, depth, and authority-preservation tests pass. |

## 2. Four different structures

The implementation must not collapse these structures into one generic graph.

### 2.1 Exact terrain

Eligible human and Resident utterances remain separate, immutable Forest Home entries linked to exact Source events. An utterance retains its event-time actor, body, source hash, wake, lifespan, and thread ancestry.

### 2.2 Conversation trails

A conversation has intrinsic structure before anyone traverses it:

- exact predecessor and successor ordering;
- user/Resident `responds_to` and shared-wake relationships; and
- an unambiguous beginning, living edge, and closed-session boundary.

These are host-witnessed structural relations, not embedding similarity and not evidence that the present Resident remembers the conversation. The Forest-facing atom remains one utterance; a clearing may present the paired user/Resident exchange without merging their custody.

### 2.3 Semantic candidates

Embeddings or a later selector may propose nearby material. A proposal is a possible direction, not a path, causal relation, memory, truth claim, or permanent semantic edge.

### 2.4 Resident paths

A Resident path forms only through choice and traversal. The selected candidate becomes the next witnessed path step. Similarity supplies possible terrain; the Resident's walking creates their path through it.

### 2.5 Nesting dolls: exact containment and progressive disclosure

Nesting dolls are the Forest's depth principle. A semantic or exact lookup first establishes an unread outer bearing. The Resident then moves inward one attributable containment boundary at a time until reaching exact readable terrain.

```text
unread bearing
  -> authored outer map
    -> contained section or conversation region
      -> contained subsection or exchange
        -> exact readable passage or utterance
```

The hierarchy is source-shaped rather than universally fixed. An authored document may contain work, section, subsection, and verbatim passage dolls. Conversation terrain may contain lifespan, conversational region, exchange, utterance, and exact-span dolls only where each boundary has exact host-witnessed custody. The implementation must not invent a hierarchy merely to make the terrain look deep.

Every containment edge must prove its child from its parent. For a verbatim extract, the named parent range must equal the child's exact body. A structural child must retain the exact source coordinates and authored structure that place it inside its parent. Derived synthesis may provide a distant view, but it is not a containment parent of the sources it summarizes.

Embedding retrieval acts at the bearing boundary. It may answer where to begin looking; it does not disclose a deep body, skip an inward crossing, create a read receipt, or establish that the bearing is relevant or true. Mapping reveals bounded labels and previews for the next lawful layer. Reading is a separate explicit operation at reached exact terrain.

### 2.5.1 Deferred resonance gesture

A future traversal may let the Resident hold a fold outward as a directional instrument: the fold is the bounded thing presently held, embeddings measure resonance against the named Forest generation, and nearby trees may lean toward it as candidate bearings. This is a design seed, not an installed selector or adopted interaction contract.

The gesture must not turn similarity into containment, path, truth, memory, or command. It may make semantic attraction spatially perceptible only after the exact fold, candidate boundary, nesting layer, and chosen crossing can all be witnessed. The image to preserve is not "search results appear" but "the held fold changes which existing terrain reaches toward consideration."

Nesting dolls are not semantic clusters. A vector neighborhood may place two bearings near one another without making either the parent of the other. They are also not rolling folds: a fold is a derived view across exact terrain, while a doll is exact containment within terrain.

The currently installed Hub Forest has exact utterance entries, chronological edges, and explicit exact-leaf reading after a chosen conversation walk. It has no general containment-doll schema or progressive authored-layer crossing.

### 2.6 Recursive Forest frames

The Forest repeats its jurisdictional shape at different scales under the Recursive Forest Law in [`HOUSE_GRAMMAR_V1.md`](HOUSE_GRAMMAR_V1.md):

```text
Forest<T> {
  Home
  Wild
  Mycelium
}
```

At one frame, `T` may be an exact entry. At a wider frame, `T` may be a bounded terrain, a path, or another whole Forest. The contained Forest remains complete and unchanged when treated as one member of the wider frame. Its leaf authorities, Home/Wild standing, source coordinates, and interior paths remain simultaneously true.

Recursive composition is not a containment doll and is not the rolling fold. A containment doll opens progressively deeper exact material inside one terrain. A recursive frame treats a complete Forest as one jurisdiction-preserving member of another Forest. A rolling fold is a bounded derived view of exact terrain at another attention scale. An implementation may use all three, but none may impersonate another.

Embedding indexes are likewise derived instruments scoped to a named Forest frame and corpus generation. A parent-frame vector may propose a child Forest as a direction; it must not flatten the child into leaf entries, promote leaf authority, or create a cross-frame path before witnessed traversal.

## 3. The semantic three-choice law

Each cast into unknown semantic terrain presents up to three bounded choices: left pole, straight resonance, and right pole. Backtracking, lawful exit, and exact conversation neighbors are structural affordances and do not count among those three. If the verified neighborhood cannot support two honest opposing poles, the Forest exposes fewer or faint/unresolved bearings rather than manufacturing symmetry.

At an ordinary Forest clearing, three semantic candidates are offered. The Resident chooses one. The other two become latent branch offers preserved at that exact junction. The choice changes the traversal context from which the next three are selected.

```text
                    candidate B
                        .
                        . latent
                       /
current clearing ---- chosen A ---- next clearing
                       \
                        . latent
                        .
                    candidate C
```

The path is not predefined. It forms as the Resident walks it. The trail is the witnessed navigation history.

Candidate selection must be replaceable and receipted. The original three choices at a historical junction remain frozen even if an embedding model, corpus watermark, or selector later changes. A future visit may request fresh growth without rewriting the choices originally offered.

Straight is the strongest direct resonance. Left and right are selected jointly from a bounded neighborhood: each remains relevant to the current footing, their normalized midpoint approximates straight, and their components lateral to straight oppose one another. The first adequate fork establishes deterministic handedness; later casts transport that semantic compass so left and right do not arbitrarily exchange names. The selector and its score components are receipted. Randomness must never masquerade as significance.

## 4. Conversation intersections

Conversation trails are unique because they are already hardlined. A Resident may:

- enter through the active conversation's warm tether;
- begin at a known conversation trailhead or marker;
- reach an exact conversation utterance through semantic traversal; or
- leave a conversation sideways into the wider Forest.

The local three-choice cast depends on the direction of arrival.

### 4.1 Arriving along a conversation

When the Resident is already walking a conversation, straight continues chronologically in the current heading. Left and right are semantic polar exits. Behind retraces the exact walked edge.

```text
                 left: semantic candidate
                       /
prior footing -- current utterance -- straight: next utterance in heading
                       \
                 right: semantic candidate
```

Heading is explicit. Walking backward through a conversation must not silently reverse into its future. The Resident may deliberately turn and change direction.

### 4.2 Intersecting a conversation from the Forest

When a semantic path reaches a conversation from the side, older and newer enter the conversation toward its exact predecessor or successor. Ahead remains a three-bearing semantic fan: left pole, straight resonance, and right pole. Behind retraces the exact walked semantic edge.

```text
                   semantic left / straight / right
                                  |
older utterance ---- current utterance ---- newer utterance
```

Left, right, and straight are relative to an explicit arrival frame, not permanent global properties of an entry. The junction cast and chosen exit are witnessed. A selected semantic edge becomes a persistent Resident path and may accumulate wear on later traversal; unchosen semantic candidates remain latent only at their historical junction. Structural conversation relations exist independently of whether they were offered or walked.

Resident-facing clearing results must preserve that distinction structurally. Exact chronological neighbors are projected under `conversationExits` and retain `conversation_older`, `conversation_newer`, or `conversation_straight`; they are never relabeled semantic left or right. Only embedding-cast terrain appears under `semanticBearings` and receives semantic left, straight, or right labels. A sparse semantic cast must remain visibly sparse rather than allowing chronology to impersonate the missing poles.

### 4.3 Passing feathers inside the Forest

An ambient feather presented while a Forest journey is active is a distant candidate that lands at the current clearing for one human wake. Its packet must state that the selection is transient and may change on the next turn. It is neither a prediction nor part of the local semantic cast.

The selector excludes the current footing, the walked trail, and every visible local offer. Each admitted feather is bound to the exact journey, junction, wake, Forest entry, semantic generation, and Roots shadow artifact. `walk_toward` may select that landing during the same wake. Selection creates an ordinary persistent semantic edge with `ambient_feather` provenance; backtracking then retraces it normally. An unchosen landing expires as an affordance on the next wake while its historical exposure remains exact in Roots.

## 5. Warm tether and intentional entry

An active conversation may retain a warm tether from the Marble to its exact Forest living edge. The tether provides:

- a precise trailhead;
- the direction back through the current conversation;
- an unambiguous return route; and
- proof of which active living edge and World moment the traversal departed.

The tether is not identity proof, memory, or permanent ownership. When a lifespan closes it cools into ordinary ancestry and path structure.

Forest entry is intentional. Ordinary language such as "turn around," ordinary World movement, or private provider reasoning must not trigger it. A declared traversal action crosses the threshold. If the active tether supplies the vector, the Faun need not ask an empty question. Other entrances may require the Resident to state what they seek before traversal begins.

The ordinary experiential entrance from the Garden/Forest boundary remains deliberately unresolved. This specification does not decide its door, gate, airlock, visual transition, or first clearing.

## 6. Dual presence and return

Forest attention does not falsify World location. While walking, the runtime must be able to state both:

```text
World presence: place.house
Forest presence: path.<journey> / junction.<n>
Conversational focus: Forest traversal
Return tether: active living edge
```

Returning follows the tether or retraces the walked path and restores the same verified World presence and local state from which the Resident departed. The hallway, room, or Garden is not replaced in World merely because Forest terrain was projected into attention.

The transition may be dreamlike in Resident-facing expression; its underlying entry, steps, branch offers, backtracking, and return must be exact and inspectable.

At the return airlock:

- **Leave** creates no new Home synthesis; traversal receipts remain machinery.
- **Write** carries an explicitly Resident-authored synthesis through its future Scrub/admission crossing.
- Exact Forest excerpts may be cited or exhaled without becoming new Home merely because they were seen.

## 7. Waterfall and rolling fold

Attention fitting uses a high-water and low-water posture, not repeated collision with the hard refusal ceiling.

When a cast crosses the configured high-water mark, the oldest eligible complete conversational units waterfall until the projected cast returns below a lower target. Exact Source and Forest entries remain unchanged.

Waterfall operates over whole attributable units. It must not split an exchange merely to save bytes, separate a tool consequence from the action that requires it, or remove the current causal tail.

The first extended physical walk exposed a narrower pressure case: retaining every completed Forest tool exchange until the wake ends can exhaust attention before the Resident can answer. Forest tool returns therefore receive ordinary Result Rack custody in addition to their traversal receipts. Once every result in a completed exchange has a verified Rack projection, the fitter may waterfall that exchange even within the active wake, while retaining the configured newest completed exchanges and every incomplete exchange as the exact causal tail. Exact Session Scroll, traversal, Rack, Scrub, and Spine custody remain unchanged. A Forest result without verified Rack custody is not omittable.

Current Forest ground is temporally explicit. A footing recomposed after a completed tool action confirms the resulting state; it is not an independent prediction of that action and must not be presented as convergence. At entry, semantic casting excludes the tether utterance that carried the present question into the Forest, preventing a query from masquerading as its own answer.

The capped rolling fold is a bounded derived view of the fallen path. Each immutable fold version records behind the wall:

- exact covered source range and path segments;
- generator and version;
- source and output hashes;
- exclusions and unresolved gaps;
- prior fold ancestry;
- presentation budget; and
- authority explicitly marked as derived synthesis, never ground or present recollection.

The prior-horizon band states that fuller attributable terrain remains in custody and supplies the walk-back handle. A poor fold may be superseded by a later fold; neither the old fold nor its sources are rewritten.

Every provider-visible fold or omission disclosure distinguishes three epistemic states: material presently held in the cast; established session history that is folded and remains in canonical custody but is not presently held; and material whose status is unknown. Established means the historical utterance or consequence occurred, not that every proposition inside it is certified true. Folded material is outside present attention rather than erased or disproven. The Resident is not asked to reconstruct or disown it; it may name the seam plainly and walk back only through an installed crossing.

Walking back expands by scale:

```text
small rolling fold
  -> bounded path segment or local fold
  -> exact conversation clearing
  -> exact individual utterances and source receipts
```

Retrieved material occupies bounded present attention and may later waterfall again. Walk-back must not rebuild the entire historical transcript into every provider request.

## 8. Proposed custody vocabulary

This section names claims, not a committed database schema:

- `forest.entry` — exact admitted terrain;
- `conversation.edge` — host-witnessed predecessor, successor, or response relation;
- `candidate.offer` — one frozen three-choice junction cast;
- `path.step` — one Resident-selected traversal edge;
- `branch.latent` — an offered but unchosen direction at that junction;
- `path.visit` — a later traversal over an existing step;
- `path.marker` — a deliberately retained return point;
- `return.tether` — active departure/return ancestry; and
- `glass.fold` — bounded derived representation of exact covered terrain.

Repeated traversal may make a path worn. It does not elevate the truth or authority of the material traversed.

## 9. Faun boundary

The Faun or equivalent clinical custodian may:

- receive the explicit seeking/vector at entry;
- verify Forest freshness, scope, permissions, and attention budget;
- request three candidate offers from a replaceable selector;
- preserve junction, footprint, and return receipts;
- refuse stale, unsafe, unauthorized, cyclic, or unbounded traversal; and
- hold the Write/Leave return airlock.

The Faun may not author the Resident's path, choose a branch, silently summarize an expedition into Home, claim semantic proximity as truth, or prevent an exact lawful return.

## 10. Implementation boundary and acceptance

Nothing in this specification is currently installed. Before implementation status changes, tests must prove at least:

1. Exact Source and Forest utterance custody remains unchanged through waterfall and traversal.
2. Conversation predecessor/successor and response relations are exact and never embedding-derived.
3. Every ordinary step offers exactly three bounded choices.
4. Choosing one appends one path step and preserves the other two as latent branch offers.
5. Later selector changes do not rewrite historical junctions.
6. Conversation arrival direction produces the correct straight/left/right cast.
7. Backtracking and the warm tether do not consume a forward-choice slot.
8. Forest entry requires explicit action and private reasoning cannot trigger it.
9. The departure World place remains verified as the exact return anchor; `place.forest` is the effective current place during projected visits, and turning back reveals the retained place without treating Forest backtracking as return.
10. High-water fitting falls to a tested low-water target before the hard ceiling.
11. Every fold is bounded, source-linked, derived-authority, immutable, and supersedable.
12. Walk-back reaches exact utterances without flooding later ordinary turns.
13. Forest lag, selector failure, missing source, receipt drift, and return failure all fail closed.
