// Compatibility imports for callers predating the room.workshop package.
export { buildRecipeEnvironment, buildRecipeInvocation, listRecipes, RecipeRunner } from '../places/hub/workshop/recipes.js';
