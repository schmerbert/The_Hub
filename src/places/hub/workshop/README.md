# Workshop room package

This is the reference package seam for an installed specialist room. It gathers the Workshop declaration, repository adapter, Git, recipes, Sandbox Bay, promotion, and path law behind one room-owned import surface.

The implementation remains at its historical paths temporarily so existing tests, stored source pointers, compatibility imports, and migration tooling do not change in a structural-only refactor. New composition code imports this package. Later moves must keep the old paths as explicit facades until the compatibility register permits removal.

Universal World event append/replay, Gateway authorization, Scrub, Result Rack, and provider presentation do not belong to this package.
